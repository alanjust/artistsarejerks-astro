// form-interactions.ts — no-op
// Mode and prompt selection now happen on /hidden-grammar/modes/[mode]
// and are passed to ai-analyze via URL parameters.

export function initFormInteractions() {
  // intentionally empty — selection flow moved to modes/[mode].astro
}
