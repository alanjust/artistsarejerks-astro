---
const currentPath = Astro.url.pathname;

const navItems = [
  { label: 'Overview', href: '/hidden-grammar' },
  { label: 'AI Analysis', href: '/hidden-grammar/ai-analyze' },
  // { label: 'Manual Analysis', href: '/hidden-grammar/analyze' },
];

const moreItems = [
  { label: 'The Framework', href: '/hidden-grammar/framework' },
  { label: 'The 11 Roots', href: '/hidden-grammar/roots' },
  { label: 'The 46 Principles', href: '/hidden-grammar/principles' },
];
---

<nav class="hg-nav">
  <div class="hg-nav-header">
    <h3>Hidden Grammar</h3>
  </div>
  <ul class="hg-nav-list">
    {navItems.map(item => (
      <li class="hg-nav-item">
        <a
          href={item.href}
          class:list={['hg-nav-link', { active: currentPath === item.href || currentPath === item.href + '/' }]}
        >
          {item.label}
        </a>
      </li>
    ))}

    <li class="hg-nav-item hg-nav-submenu">
      <button class="hg-nav-label hg-nav-toggle" type="button">
        More
        <svg class="hg-nav-chevron" xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <polyline points="6 9 12 15 18 9"></polyline>
        </svg>
      </button>
      <ul class="hg-nav-sublist">
        {moreItems.map(item => (
          <li class="hg-nav-subitem">
            <a
              href={item.href}
              class:list={['hg-nav-link hg-nav-sublink', { active: currentPath === item.href || currentPath === item.href + '/' }]}
            >
              {item.label}
            </a>
          </li>
        ))}
      </ul>
    </li>
  </ul>
</nav>

<style>
  /* Box-sizing for navigation */
  .hg-nav *,
  .hg-nav *::before,
  .hg-nav *::after {
    box-sizing: border-box;
  }

  .hg-nav {
    background: var(--color-white);
    border-right: 2px solid var(--color-gray-2);
    padding: clamp(1.5rem, 4vw, 3rem);
    height: 100%;
    min-width: 240px;
    position: sticky;
    top: 0;
  }

  .hg-nav-header {
    margin-bottom: clamp(1rem, 3vw, 1.5rem);
    padding-bottom: clamp(0.75rem, 2vw, 1rem);
    border-bottom: 2px solid var(--color-purple);
  }

  .hg-nav-header h3 {
    font-family: var(--font-display);
    font-size: clamp(1.125rem, 2.5vw, 1.25rem);
    color: var(--color-purple);
    margin: 0;
  }

  .hg-nav-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .hg-nav-item {
    margin-bottom: clamp(0.375rem, 1vw, 0.5rem);
  }

  .hg-nav-link {
    display: block;
    padding: clamp(0.5rem, 1.5vw, 0.75rem) clamp(0.75rem, 2vw, 1rem);
    color: var(--color-gray-8);
    text-decoration: none;
    border-radius: 6px;
    transition: all 0.2s ease;
    font-family: var(--font-body);
    font-size: clamp(0.875rem, 2vw, 1rem);
    min-height: 44px;
    display: flex;
    align-items: center;
  }

  .hg-nav-link:hover {
    background: var(--color-cream);
    color: var(--color-purple);
  }

  .hg-nav-link.active {
    background: var(--color-purple);
    color: var(--color-white);
    font-weight: 600;
  }

  /* Submenu styles */
  .hg-nav-submenu {
    margin-top: clamp(0.75rem, 2vw, 1rem);
  }

  .hg-nav-toggle {
    display: flex;
    align-items: center;
    justify-content: space-between;
    width: 100%;
    padding: clamp(0.5rem, 1.5vw, 0.75rem) clamp(0.75rem, 2vw, 1rem);
    color: var(--color-gray-6);
    font-family: var(--font-body);
    font-size: clamp(0.75rem, 1.75vw, 0.875rem);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    background: none;
    border: none;
    border-radius: 6px;
    cursor: pointer;
    transition: all 0.2s ease;
    min-height: 44px;
  }

  .hg-nav-toggle:hover {
    background: var(--color-cream);
    color: var(--color-purple);
  }

  .hg-nav-chevron {
    transition: transform 0.2s ease;
  }

  .hg-nav-submenu.open .hg-nav-chevron {
    transform: rotate(180deg);
  }

  .hg-nav-sublist {
    list-style: none;
    padding: 0;
    margin: 0;
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.3s ease;
  }

  .hg-nav-submenu.open .hg-nav-sublist {
    max-height: 500px;
  }

  .hg-nav-subitem {
    margin-bottom: clamp(0.375rem, 1vw, 0.5rem);
  }

  .hg-nav-sublink {
    padding-left: clamp(2rem, 5vw, 3rem);
    font-size: clamp(0.8125rem, 1.75vw, 0.875rem);
  }

  /* Mobile responsive */
  @media (max-width: 1023px) {
    .hg-nav {
      position: static;
      min-width: auto;
      width: 100%;
      border-right: none;
      border-bottom: 2px solid var(--color-gray-2);
      padding: clamp(0.75rem, 3vw, 1rem);
    }

    .hg-nav-list {
      display: flex;
      flex-wrap: wrap;
      gap: clamp(0.5rem, 1.5vw, 0.75rem);
    }

    .hg-nav-item {
      margin-bottom: 0;
      flex: 1 1 auto;
    }

    .hg-nav-link {
      padding: clamp(0.5rem, 1.5vw, 0.625rem) clamp(0.625rem, 2vw, 0.75rem);
      font-size: clamp(0.8125rem, 2vw, 0.875rem);
      justify-content: center;
      text-align: center;
    }

    .hg-nav-submenu {
      flex-basis: 100%;
      margin-top: clamp(0.5rem, 1.5vw, 0.75rem);
    }

    .hg-nav-sublist {
      display: flex;
      flex-wrap: wrap;
      gap: clamp(0.5rem, 1.5vw, 0.75rem);
      padding-top: clamp(0.5rem, 1.5vw, 0.75rem);
    }

    .hg-nav-subitem {
      margin-bottom: 0;
      flex: 1 1 auto;
    }

    .hg-nav-sublink {
      padding-left: clamp(0.625rem, 2vw, 0.75rem);
      justify-content: center;
      text-align: center;
    }
  }
</style>

<script>
  // Handle dropdown toggle
  const toggleBtn = document.querySelector('.hg-nav-toggle');
  const submenu = document.querySelector('.hg-nav-submenu');

  toggleBtn?.addEventListener('click', () => {
    submenu?.classList.toggle('open');
  });
</script>
