---
export const prerender = false;
// The old guided setup now lives in the artist workspace.
const artist = Astro.url.searchParams.get('artist');
return Astro.redirect(artist ? `/prototype/workspace/member/?artist=${encodeURIComponent(artist)}` : '/join/?kind=artist');
---
