---
export const prerender = false;
// Showings are now added from the Showings tab in the artist workspace.
const artist = Astro.url.searchParams.get('artist');
return Astro.redirect(artist ? `/prototype/workspace/member/?artist=${encodeURIComponent(artist)}#showing` : '/join/?kind=artist');
---
